# 应用非敏感配置（可明文）
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: meau
data:
  # ===== 对话 LLM（DeepSeek）=====
  OPENAI_BASE_URL: https://api.deepseek.com/v1
  OPENAI_MODEL: deepseek-v4-flash

  # ===== 向量模型（千问 DashScope）=====
  EMBEDDING_BASE_URL: https://dashscope.aliyuncs.com/compatible-mode/v1
  EMBEDDING_MODEL: text-embedding-v3

  # ===== 数据库通用配置 =====
  USE_DATABASE: "true"
  DB_POOL_SIZE: "20"
  DB_POOL_MIN_CACHED: "2"
  DB_POOL_MAX_CACHED: "5"
  DB_CONN_RECYCLE: "1800"
  DB_MAX_EXECUTION_MS: "5000"
  DB_DISH_LIMIT: "1000"

  # ===== Redis（集群内 Service DNS）=====
  REDIS_URL: redis://redis.meau.svc.cluster.local:6379/0

  # ===== 高并发 =====
  WORKERS: "4"
  MAX_CONCURRENT_CHATS: "20"
  LLM_REQUEST_TIMEOUT: "30"

  # ===== 会话与限流 =====
  SESSION_TTL_SECONDS: "1800"
  SESSION_CLEANUP_INTERVAL: "300"
  MAX_SESSIONS: "500"
  CHAT_RATE_PER_SESSION: "30"
  CHAT_RATE_PER_IP: "60"
  CHAT_RATE_WINDOW: "60"
  CART_RATE_PER_SESSION: "10"
  CART_RATE_PER_IP: "20"
  CART_RATE_WINDOW: "60"
  SESSION_OPS_RATE_PER_SESSION: "10"
  SESSION_OPS_RATE_WINDOW: "60"

  # ===== CORS 跨域来源（逗号分隔，按需修改为你的前端域名）=====
  CORS_ALLOWED_ORIGINS: http://localhost:3000,http://127.0.0.1:3000

  # ===== 可信反向代理 =====
  # NodePort 模式下，Pod 直连客户端 IP，无需信任 XFF
  TRUSTED_PROXIES: ""

  # ===== 日志级别 =====
  LOG_LEVEL: INFO

  # ===== 收钱吧网关 =====
  SQB_APP_ID: "202607161527269102513553408"
  SQB_GATEWAY_URL: https://test-gateway-openapi-kaci.shouqianba.com/api/1.0
  SQB_VERSION: "1.0"

  # ===== Gunicorn 调优 =====
  GUNICORN_TIMEOUT: "60"
  GUNICORN_GRACEFUL_TIMEOUT: "75"
  GUNICORN_MAX_REQUESTS: "1000"
  GUNICORN_MAX_REQUESTS_JITTER: "50"
