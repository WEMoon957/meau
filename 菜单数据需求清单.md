# 菜单数据需求清单

> 基于端到端测试（50条参数化 + 20条自然语言）发现的P0/P1数据问题，以及项目硬约束梳理
> 日期：2026-07-30
> 修订：2026-07-30 经向量库（ChromaDB）核查，发现数据存在**双源脱节**问题，本清单已更新数据来源与回填方向

---

## 〇、数据架构现状（关键背景）

核查发现辣度/过敏原等属性数据存在**双源脱节**，向量库与MySQL各存一份且不对齐：

| 属性 | 向量库（ChromaDB，81道档案） | MySQL（dishes表，90道） | recommend_dishes过滤用哪个 | 问题 |
|------|---------------------------|----------------------|-------------------------|------|
| 辣度 `spice_level` | 80/81有标注（99%） | 部分缺失 | **MySQL** | MySQL缺失的菜品过滤失效 |
| 过敏原 `allergen_info` | **69/81完整标注**（85%） | **仅1道有标注**（1%） | **MySQL** | 过敏原过滤基本失效 |
| 过敏原格式 | "不含香菜，不含葱，不含蒜..." 结构化文本 | list，基本全空 | - | 格式不一致 |
| 菜品覆盖 | 81道 | 90道（9道仅在MySQL） | MySQL | 9道菜无法用向量库检索 |
| 人群 `suitable_crowd` | 69条（dish_crowd类型） | 部分标注 | MySQL | 双源不一致 |

**结论**：向量库是辣度/过敏原的**权威数据源**，但代码用MySQL做结构化过滤，导致过滤基本失效。**根本修复方向是双源同步**：以向量库为权威源，回填MySQL结构化字段，或改用向量库做过敏原过滤。

**关键纠错**：向量库明确标注"傣味香茅草烤鱼：不含海鲜"（淡水鱼调料，非海鲜过敏原）。之前test_09的P0修复用菜名兜底过滤"鱼"字，属于**误杀**，应改用向量库的结构化过敏原信息。

---

## 一、P0级数据问题（必须修复，阻塞业务硬约束）

### P0-D1: 过敏原数据双源脱节，MySQL几乎全空

| 项目 | 内容 |
|------|------|
| **问题** | 向量库69/81道有完整过敏原标注，但MySQL仅1道有标注；recommend_dishes按MySQL过滤，过敏原过滤基本失效 |
| **影响** | 过敏原过滤失效，可能向过敏用户推荐危险菜品 |
| **根因** | 数据录入时向量库与MySQL未同步，向量库的allergen_info未回填到MySQL的allergens字段 |
| **修复方向** | 将向量库的allergen_info解析后回填MySQL，或改用向量库做过敏原过滤 |

**过敏原标准清单（必须覆盖）**：

| 过敏原类别 | 数据库存储值 | 菜名兜底关键词（已有） | 需核查的菜品特征 |
|-----------|-------------|---------------------|----------------|
| 海鲜 | `海鲜` | 鱼/虾/蟹/贝/螺/鱿鱼/墨鱼/章鱼/扇贝/蛤/蚝/鲍 | 含水产食材的菜品 |
| 花生 | `花生` | 花生 | 宫保类、麻辣类、凉菜类 |
| 鸡蛋 | `鸡蛋` | 鸡蛋/蛋 | 含蛋的菜品（蛋糕、蛋汤、滑蛋） |
| 牛奶 | `牛奶` | 牛奶/芝士/黄油/奶油/双皮奶 | 含乳制品的甜品、饮品 |
| 大豆 | `大豆` | 大豆/豆腐/豆干/豆浆 | 豆制品菜品 |
| 大蒜 | `大蒜` | 大蒜/蒜蓉 | 蒜蓉类菜品（锅底需重点核查） |
| 麸质 | `麸质` | （无兜底） | 含面粉/面食的菜品 |
| 坚果 | `坚果` | （无兜底） | 含腰果/松子/核桃的菜品 |

**数据补全率目标**：`allergens` 字段非空率 **100%**（无过敏原的菜品填 `[]` 或空字符串，但不能是NULL）

**双源同步修复方案（P0-D1 根本修复）**：

向量库已存的69道过敏原信息需解析回填MySQL，具体步骤：

1. **从向量库提取**：读取 `dish_profile` 类型的 `allergen_info` 字段
2. **解析结构化文本**：将"不含香菜，不含葱，不含蒜，不含花生，不含海鲜，不含乳制品"解析为：
   - 含过敏原的菜品 → `allergens = ["海鲜", "花生"]`（向量库标注"含X"的）
   - 不含过敏原的菜品 → `allergens = []`（向量库标注"不含X"的）
3. **回填MySQL**：按菜名匹配，UPDATE dishes SET allergens = ... WHERE name = ...
4. **补全剩余菜品**：向量库无档案的9道菜（仅在MySQL中），需人工核查食材补标

**回填脚本示例**：
```python
# 从向量库读取过敏原信息，回填MySQL
from kb_store import VectorStore
store = VectorStore()
store.load()
profiles = store.get_all_by_type("dish_profile")

for p in profiles:
    name = p['metadata'].get('dish_name', '')
    allergen_info = p['metadata'].get('allergen_info', '')
    # 解析"含X"的过敏原（向量库格式："含花生，不含海鲜"）
    contains = []
    for allergen in ["香菜", "葱", "蒜", "花生", "海鲜", "乳制品", "鸡蛋", "大豆", "麸质"]:
        if f"含{allergen}" in allergen_info and f"不含{allergen}" not in allergen_info:
            contains.append(allergen)
    # UPDATE dishes SET allergens = %s WHERE name = %s
    # ... 执行SQL更新
```

**重要纠错**：之前在 `tools.py` 加的菜名关键词兜底过滤（`_ALLERGEN_NAME_KEYWORDS`）应**移除或改为只在双源都无数据时兜底**，因为它会误杀"傣味香茅草烤鱼"这类向量库明确标注"不含海鲜"的菜品。

---

### P0-D2: 锅底数据不足且分类错误

| 项目 | 内容 |
|------|------|
| **问题** | 有效锅底仅1种（"菌汤生态鸡子母锅"），且"山茅野菜拼盘"被误分类为菌汤锅底 |
| **影响** | 所有推荐都返回同一锅底；大蒜过敏时无锅底可选（违反硬约束） |
| **修复要求** | 补充4-6种不同口味锅底；纠正分类错误 |

**锅底数据要求**：

| 要求 | 说明 |
|------|------|
| 数量 | 4-6种不同口味锅底 |
| 口味覆盖 | 不辣（番茄/菌汤/清汤）、微辣、中辣、特辣（麻辣牛油）、鸳鸯锅 |
| 过敏原覆盖 | 至少2种不含大蒜的锅底；至少2种不含花生的锅底 |
| 分类正确 | 所有锅底必须在"菌汤锅底"分类且菜名含"锅"字 |
| 非锅底纠正 | "山茅野菜拼盘"等非锅底菜品移至正确分类（如"山茅野菜"） |

**建议锅底数据（需录入）**：

| 菜名 | 价格 | 辣度 | 过敏原 | 说明 |
|------|------|------|--------|------|
| 菌汤生态鸡子母锅 | 118 | 不辣 | 大蒜 | 已有，保留 |
| 番茄养生锅 | 88 | 不辣 | - | 新增 |
| 麻辣牛油锅 | 98 | 特辣 | 花生 | 新增 |
| 野生菌汤锅 | 108 | 不辣 | - | 新增 |
| 清汤鸳鸯锅 | 78 | 不辣 | - | 新增（半辣半不辣） |
| 酸菜白肉锅 | 68 | 微辣 | - | 新增 |

---

### P0-D3: 价格数据缺失或为0

| 项目 | 内容 |
|------|------|
| **问题** | 部分菜品 `price` 为 0.0 或 NULL |
| **影响** | 推荐结果合计错误，违反"价格数据完整"硬约束 |
| **修复要求** | 所有菜品 `price` 必须 > 0 |

**核查SQL**：
```sql
SELECT id, name, price FROM dishes WHERE price IS NULL OR price <= 0;
```
**目标**：查询结果为空

---

## 二、P1级数据问题（影响推荐质量）

### P1-D1: 健康标签覆盖率不足

| 项目 | 内容 |
|------|------|
| **问题** | `dietary_tags` 字段标注率低，"高蛋白"/"素食"/"低脂"标签的菜品不足 |
| **影响** | 健康标签筛选后候选池清空，仅推荐锅底（test_15/16/17/18 失败） |
| **修复要求** | 补全健康标签，确保每类标签至少有15道菜品 |

**健康标签标准清单**：

| 标签 | 定义 | 需要标注的菜品特征 | 目标数量 |
|------|------|-------------------|---------|
| 高蛋白 | 蛋白质含量高 | 牛肉/羊肉/鸡肉/鱼/虾/豆腐类菜品 | ≥15道 |
| 低脂 | 脂肪含量低 | 蔬菜/菌菇/清汤类菜品 | ≥15道 |
| 素食 | 无肉类 | 蔬菜/菌菇/豆制品菜品 | ≥15道 |
| 低糖 | 糖分低 | 无糖饮品/清淡菜品 | ≥10道 |
| 养胃 | 易消化 | 粥/汤/清淡菜品 | ≥10道 |

**核查SQL**：
```sql
SELECT 
  SUM(CASE WHEN dietary_tags LIKE '%高蛋白%' THEN 1 ELSE 0 END) AS high_protein,
  SUM(CASE WHEN dietary_tags LIKE '%低脂%' THEN 1 ELSE 0 END) AS low_fat,
  SUM(CASE WHEN dietary_tags LIKE '%素食%' THEN 1 ELSE 0 END) AS vegetarian,
  SUM(CASE WHEN dietary_tags LIKE '%低糖%' THEN 1 ELSE 0 END) AS low_sugar
FROM dishes;
```

---

### P1-D2: 辣度数据双源不一致

| 项目 | 内容 |
|------|------|
| **问题** | 向量库80/81道有辣度标注（`spice_level`元数据），MySQL部分缺失；两源存在格式差异（向量库有"微辣(芥末)"等扩展值） |
| **影响** | 口味筛选失效，"特辣"请求可能推荐不辣菜品 |
| **修复要求** | 以向量库为权威源，回填MySQL；统一辣度枚举 |

**辣度标准枚举**：

| 标准值 | 说明 |
|--------|------|
| `不辣` | 无辣味 |
| `微辣` | 轻微辣味 |
| `中辣` | 中等辣度 |
| `特辣` | 重度辣味 |

**核查SQL**：
```sql
SELECT id, name, spicy_level FROM dishes 
WHERE spicy_level IS NULL 
   OR spicy_level NOT IN ('不辣', '微辣', '中辣', '特辣');
```
**目标**：查询结果为空

---

### P1-D3: suitable_for 字段覆盖不全

| 项目 | 内容 |
|------|------|
| **问题** | `suitable_for` 字段未覆盖所有人群场景 |
| **影响** | 人群筛选失效，聚餐可能推荐儿童套餐 |
| **修复要求** | 补全 `suitable_for` 字段，覆盖所有人群标签 |

**人群标签标准清单**：

| 标签 | 适用菜品特征 | 注意事项 |
|------|------------|---------|
| 聚餐 | 适合多人分享的大份菜品 | 默认适用大部分菜品 |
| 情侣 | 适合2人精致用餐 | 中小份、精致菜品 |
| 一人食 | 适合单人用餐 | 小份、简餐 |
| 老人 | 易消化、清淡 | 不辣、软烂 |
| 儿童 | 适合儿童口味 | 不辣、营养均衡、菜名含"儿童"的专享菜品 |
| 孕妇 | 适合孕妇营养 | 高蛋白、无活血食材、全熟 |

**儿童菜品隔离规则**：
- 菜名含"儿童"的菜品，`suitable_for` 只能含"儿童"
- 不得同时标注"聚餐"/"情侣"/"一人食"

**核查SQL**：
```sql
-- 儿童菜品隔离检查
SELECT id, name, suitable_for FROM dishes 
WHERE name LIKE '%儿童%' 
  AND (suitable_for LIKE '%聚餐%' OR suitable_for LIKE '%情侣%' OR suitable_for LIKE '%一人食%');
```
**目标**：查询结果为空

---

### P1-D4: 季节与天气标签缺失

| 项目 | 内容 |
|------|------|
| **问题** | `seasons` 和 `weather_tags` 字段大量为空 |
| **影响** | 天气季节推荐失效，夏季可能推荐冬季菜品 |
| **修复要求** | 补全季节和天气标签 |

**标准枚举**：

| 字段 | 标准值 |
|------|--------|
| `seasons` | `春`, `夏`, `秋`, `冬`（可多选，空数组表示四季适用） |
| `weather_tags` | `热天`, `冷天`, `雨天`（可多选，空数组表示不限天气） |

**标注规则**：
- 汤锅类、滋补类 → 冬季/冷天
- 凉菜、冰饮类 → 夏季/热天
- 菌菇类 → 秋季（野生菌时令）
- 常规菜品 → 四季适用（留空）

---

## 三、P2级数据问题（数据完整性）

### P2-D1: gross_margin 字段缺失

| 项目 | 内容 |
|------|------|
| **问题** | `init_db.py` 创建的表无 `gross_margin` 字段，部分菜品该值为NULL |
| **影响** | 推荐评分无法使用毛利率权重 |
| **修复要求** | 所有菜品 `gross_margin` 在 0~1 之间 |

**核查SQL**：
```sql
SELECT id, name, gross_margin FROM dishes 
WHERE gross_margin IS NULL OR gross_margin < 0 OR gross_margin > 1;
```
**目标**：查询结果为空

---

### P2-D2: signature 字段标注不准

| 项目 | 内容 |
|------|------|
| **问题** | `is_signature` 字段标注可能不准，部分招牌菜未标注 |
| **影响** | 推荐结果缺少招牌菜权重 |
| **修复要求** | 核查招牌菜标注，每类至少1-2道招牌菜 |

---

### P2-D3: description 字段质量

| 项目 | 内容 |
|------|------|
| **问题** | `description` 字段部分为空或过于简短 |
| **影响** | 知识库查询返回的信息不完整 |
| **修复要求** | 每道菜品 `description` 至少20字，含主要食材和烹饪方式 |

---

## 四、数据完整性校验脚本

建议录入完成后运行以下校验脚本：

### 4.1 向量库核查脚本（权威源）

```python
# check_kb_data.py - 向量库数据完整性核查
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from kb_store import VectorStore

store = VectorStore()
store.load()
profiles = store.get_all_by_type("dish_profile")

# 1. 过敏原覆盖率
allergen_coverage = sum(1 for p in profiles if p['metadata'].get('allergen_info'))
print(f"过敏原覆盖率: {allergen_coverage}/{len(profiles)} ({allergen_coverage*100//len(profiles)}%)")
# 目标: 100%

# 2. 辣度覆盖率
spicy_coverage = sum(1 for p in profiles if p['metadata'].get('spice_level'))
print(f"辣度覆盖率: {spicy_coverage}/{len(profiles)} ({spicy_coverage*100//len(profiles)}%)")
# 目标: 100%

# 3. 人群覆盖
crowd_coverage = sum(1 for p in profiles if p['metadata'].get('suitable_crowd'))
print(f"人群覆盖率: {crowd_coverage}/{len(profiles)} ({crowd_coverage*100//len(profiles)}%)")

# 4. 菜品数量
dish_names = set(p['metadata'].get('dish_name', '') for p in profiles)
print(f"向量库菜品数: {len(dish_names)}")
```

### 4.2 MySQL核查脚本（回填后校验）

```sql
-- ===== P0 校验 =====
-- 1. 过敏原非空率（回填后应达100%）
SELECT
  COUNT(*) AS total,
  SUM(CASE WHEN allergens IS NULL OR allergens = '' THEN 1 ELSE 0 END) AS null_allergens,
  ROUND(SUM(CASE WHEN allergens IS NOT NULL AND allergens != '' THEN 1 ELSE 0 END) * 100.0 / COUNT(*), 1) AS coverage_pct
FROM dishes;
-- 目标: null_allergens=0, coverage_pct=100

-- 2. 锅底数量
SELECT COUNT(*) FROM dishes
WHERE category = '菌汤锅底' AND name LIKE '%锅%';
-- 目标: >= 4

-- 3. 价格完整性
SELECT COUNT(*) FROM dishes WHERE price IS NULL OR price <= 0;
-- 目标: 0

-- ===== P1 校验 =====
-- 4. 健康标签覆盖率
SELECT
  SUM(CASE WHEN dietary_tags LIKE '%高蛋白%' THEN 1 ELSE 0 END) AS high_protein,
  SUM(CASE WHEN dietary_tags LIKE '%低脂%' THEN 1 ELSE 0 END) AS low_fat,
  SUM(CASE WHEN dietary_tags LIKE '%素食%' THEN 1 ELSE 0 END) AS vegetarian
FROM dishes;
-- 目标: 每项 >= 15

-- 5. 辣度枚举一致性（回填后应全部在枚举内）
SELECT COUNT(*) FROM dishes
WHERE spicy_level NOT IN ('不辣', '微辣', '中辣', '特辣');
-- 目标: 0

-- 6. 儿童菜品隔离
SELECT COUNT(*) FROM dishes
WHERE name LIKE '%儿童%'
  AND (suitable_for LIKE '%聚餐%' OR suitable_for LIKE '%情侣%');
-- 目标: 0

-- ===== P2 校验 =====
-- 7. 毛利率完整性
SELECT COUNT(*) FROM dishes
WHERE gross_margin IS NULL OR gross_margin < 0 OR gross_margin > 1;
-- 目标: 0

-- 8. 描述完整性
SELECT COUNT(*) FROM dishes
WHERE description IS NULL OR LENGTH(description) < 20;
-- 目标: 0
```

### 4.3 双源一致性核查脚本

```python
# check_dual_source.py - 向量库 vs MySQL 一致性核查
import os, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "main"))
from kb_store import VectorStore
from menu_data import get_all_dishes

store = VectorStore()
store.load()
profiles = store.get_all_by_type("dish_profile")
mysql_dishes = {d.name: d for d in get_all_dishes()}

mismatches = []
for p in profiles:
    name = p['metadata'].get('dish_name', '')
    mysql_dish = mysql_dishes.get(name)
    if not mysql_dish:
        continue

    # 辣度对比
    kb_spicy = p['metadata'].get('spice_level', '')
    if kb_spicy and kb_spicy != mysql_dish.spicy_level:
        mismatches.append(f"辣度: {name} KB={kb_spicy} MySQL={mysql_dish.spicy_level}")

    # 过敏原对比（解析向量库的allergen_info）
    kb_allergen_info = p['metadata'].get('allergen_info', '')
    kb_contains = [a for a in ["花生","海鲜","鸡蛋","牛奶","大豆","大蒜","麸质"]
                   if f"含{a}" in kb_allergen_info and f"不含{a}" not in kb_allergen_info]
    mysql_allergens = mysql_dish.allergens or []
    if set(kb_contains) != set(mysql_allergens):
        mismatches.append(f"过敏原: {name} KB={kb_contains} MySQL={mysql_allergens}")

print(f"双源不一致: {len(mismatches)}处")
for m in mismatches[:20]:
    print(f"  {m}")
# 目标: 0处不一致
```

---

## 五、数据补全优先级与排期建议

| 优先级 | 数据项 | 工作量估算 | 阻塞问题 |
|--------|--------|-----------|---------|
| **P0** | 过敏原字段补全（90道菜品逐道核查） | 大 | 过敏原泄漏（已临时兜底） |
| **P0** | 锅底数据补充（新增4-5种） | 中 | 推荐单一、过敏无锅底可用 |
| **P0** | 价格数据核查 | 小 | 合计金额错误 |
| **P1** | 健康标签补全 | 大 | 健康场景推荐失效 |
| **P1** | 辣度数据核查 | 小 | 口味筛选失效 |
| **P1** | suitable_for 补全 | 中 | 人群筛选失效 |
| **P1** | 季节天气标签补全 | 中 | 天气推荐失效 |
| **P2** | gross_margin 补全 | 小 | 毛利率权重失效 |
| **P2** | signature 核查 | 小 | 招牌权重失效 |
| **P2** | description 补全 | 中 | 知识库查询质量 |

---

## 六、数据录入模板

建议按以下模板录入新菜品数据（CSV格式）：

```csv
name,price,category,spicy_level,suitable_for,dietary_tags,allergens,description,is_signature,seasons,weather_tags,gross_margin
番茄养生锅,88,菌汤锅底,不辣,"聚餐|老人|儿童",低脂,,"番茄锅底，酸甜开胃，适合老人儿童",0,"春|秋",,0.68
麻辣牛油锅,98,菌汤锅底,特辣,聚餐,,"花生","正宗牛油麻辣锅，麻辣鲜香",0,冬,冷天,0.70
```

**字段规范**：
- 多值字段用 `|` 分隔（如 `suitable_for="聚餐|老人|儿童"`）
- 空值留空（如 `allergens=` 表示无过敏原）
- 布尔值用 `1`/`0`
- 价格为数字，无单位
- `gross_margin` 为 0~1 的小数
